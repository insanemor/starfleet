# Story 3.5: Gate de catálogo

Status: done

Implementação: `catalogGate.ts`, comando `catalog-check`, script `npm run catalog:check`. Módulos com `promoted: true` exigem `README.md` e `tests/smoke` no diretório do módulo. Erro `CATALOG_QUALITY_GATE_FAILED` (exit 30).
